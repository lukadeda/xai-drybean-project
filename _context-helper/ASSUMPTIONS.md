# ASSUMPTIONS

- `XAI_Projektarbeit 19.54.42/` is the current authoritative local report export because the user confirmed this Overleaf-copy workflow and this folder is the newest checked export.
- The user wanted a root-level material-context workspace, not an update to `peer-review/_context-helper/`.
- The user-mentioned folders `lab`, `lab2`, and `paper vortrag2` are to be low priority if present; they were not present as root folders during inspection.
- Paper/literature folders are supporting context only unless the user asks for literature work.
- No original source files should be changed during this context-building pass.
