# OPEN QUESTIONS

- Has the user imported a newer Overleaf export than `XAI_Projektarbeit 19.54.42/`? If yes, switch authoritative checks to that newer export.
- Should the XAI chapter explain only the Random Forest model, or add a short justification that the best MLP was not chosen as the main XAI target because tree models are more practical for SHAP and preserve raw feature scales?
- Should the local XAI example remain the current correctly classified `DERMASON` instance, or should additional local examples be added for a confusion case such as `DERMASON`/`SIRA`?
- Should `notebooks/modelvergleich.tex` be regenerated again before final submission, or is the current report/`02_model_training.ipynb` table authoritative?
- Are there formal requirements in `Projektarbeit_Anforderungen.pdf` that must be converted and indexed before final writing?
