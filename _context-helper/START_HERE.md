# START HERE

This is the root material context for `/Users/lukadeda/xai/projektarbeit`.

Primary focus: the XAI Dry Bean classification project outside the lower-priority folders named by the user (`lab`, `lab2`, `paper vortrag2`). In this workspace, no such root folders were present; the closest low-priority areas are literature/paper folders and the already-contextualized `peer-review/` folder, which were only skimmed.

Best current entry points:

- `CONTEXT_PACK.md` for compact project memory.
- `IMPORTANT_FILES.md` for what to read first.
- `EVIDENCE_INDEX.md` for grounded claims and source locators.
- `OPEN_QUESTIONS.md` and `NEXT_STEPS.md` for what remains to do.

Current assessment: the project is a German XAI report on multiclass Dry Bean classification using selected morphological features.

User workflow: the report is edited in Overleaf and then copied into this workspace for checking. Always inspect the newest local `XAI_Projektarbeit*` export folder first. The currently confirmed newest export is `XAI_Projektarbeit 19.54.42/`.

Context structure: this root `_context-helper/` is the overarching context for the projectarbeit. The separate `peer-review/_context-helper/` can remain separate because peer review is a distinct workstream with its own sources and briefs; the root context should only reference it when needed.

Writing preference: when drafting or revising report prose, use the `human-voice-writer` style rules. Keep the language natural, technical, and not visibly AI-written.

Evidence rule: report arguments and methodological justifications must be grounded in checked sources. Use concrete references, chapter/page/section locators where possible, and do not invent support from memory.

Submission scope: both the report and the Jupyter notebook matter. The notebook must run and its metrics/figures must match the values reported in the report.

The most incomplete sections in the current report export are XAI results, evaluation/discussion, conclusion, and abstract.
